{{--
    Admin panel scripts. app.js is plain, dependency-free JavaScript that wires
    the sidebar, dropdowns, modals, tabs, table sort/search and toasts through
    event delegation — so markup added later needs no extra binding.
--}}
<script src="{{ asset('backend/template/js/app.js') }}" defer></script>
